// Live Game — the main screen. Score, clock, fouls, timeouts, period,
// possession, all driven purely by state_update. Touch-lock, score
// attribution popup, and court-tap capture all live here.
// Placeholder.

export function LiveGame() {
    return (
        <div style={{ padding: 24 }}>
            <h1>LIVE GAME</h1>
            <p>placeholder — score/clock/fouls/timeouts driven by state_update</p>
        </div>
    );
}
