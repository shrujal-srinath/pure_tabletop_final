// Roster Setup — stats/advanced mode only. Player name/number entry per
// team, feeding the `players` array state-engine expects.
// Placeholder.

export function RosterSetup() {
    return (
        <div style={{ padding: 24 }}>
            <h1>ROSTER SETUP</h1>
            <p>placeholder — player name/number entry (stats/advanced modes only)</p>
        </div>
    );
}
