// Match Setup — collects SetupGamePayload fields and emits `setup_game`.
// Placeholder. Real version: team names/colors, period length, shot clock
// length, periods, game mode; shows setup_error inline on rejection.

export function MatchSetup() {
    return (
        <div style={{ padding: 24 }}>
            <h1>MATCH SETUP</h1>
            <p>placeholder — team/period/mode configuration form</p>
        </div>
    );
}
