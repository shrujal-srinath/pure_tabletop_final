// Settings panel — opened only after touch-lock is released. End-game
// button, mid-game corrections that don't need to be physical-button-only.
// Placeholder.

export function SettingsPanel() {
    return (
        <div style={{ padding: 24 }}>
            <h1>SETTINGS</h1>
            <p>placeholder — end game, mid-game corrections</p>
        </div>
    );
}
