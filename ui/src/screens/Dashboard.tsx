// Dashboard — landing screen once the daemon is up and no game is active.
// Placeholder. Real version: button to start Match Setup.

export function Dashboard() {
    return (
        <div style={{ padding: 24 }}>
            <h1>DASHBOARD</h1>
            <p>placeholder — no active game</p>
        </div>
    );
}
