# box-pi

THE BOX — Pi-only tabletop basketball scoring console. Raspberry Pi 4 (brains) +
Raspberry Pi Pico W (physical buttons over UART) + referee-facing and
spectator-facing touchscreens, syncing to the shared THE BOX Supabase backend so
games it scores show up correctly on the existing website and app.

This is a clean rewrite in its own repo — not a copy of the old `BOXV2-TEST`
pi-daemon. See `shared/wire-contract.js` for the event/data shapes this device
must stay compatible with.

## Progress log
- [2026-09-12] Repo created (`git init`), `.gitignore` added. Started `shared/wire-contract.js` — defines every cloud (Supabase) and LAN (daemon↔touchscreen) event name and payload shape as JSDoc typedefs + constants, no logic yet. Cloud side copies the existing website's event/field names exactly (frozen); LAN side is this project's own design.
- [2026-09-12] Added root `package.json` (`"type": "module"`) — needed so plain `.js` files actually resolve as ES modules when one imports another; without it Node treats them as CommonJS by default and imports fail.
- [2026-09-12] Built `shared/state-engine.js` — the one pure function (`reduce(state, action)`) that owns every game-state change (score, foul, timeout, clock, undo). No I/O, no Date.now(), no mutation — testable by calling it directly. Two rules worth remembering: team fouls reset every period (for the bonus), but a player's personal fouls never reset (foul-out is for the whole game); timeouts are pooled per FIBA half/OT bracket, not per period, so they only refill when the bracket itself changes. Verified with `scripts/test-state-engine.mjs` — confirmed a timeout gets rejected once a bracket's allotment is used up, a player flips `fouledOut` at their 5th foul, and `UNDO` reverts exactly one action with no redo.
- [2026-09-12] Fixed a review note in `state-engine.js`: `CLOCK_TICK` no longer creates its own UNDO point (ticks are automatic, not ref-triggered) — `UNDO` now skips past any number of ticks and reverts the last real action instead of a fraction-of-a-second-old tick. Re-verified with the same test script.
- [2026-09-12] Built `daemon/uart-bridge.js` — turns a line from the Pico into an action object, nothing more (doesn't call the reducer, doesn't touch Socket.io/Supabase). Checked the real firmware (`pico-firmware/main.py`) before writing this: the actual button board only ever sends `SCORE_A1/2/3`, `SCORE_B1/2/3`, `CLOCK_TOGGLE` (one button, not separate start/stop), `SHOT_CLOCK_24`, `UNDO`, `SETTINGS` (touchscreen lock toggle), and `PICO_READY` (boot handshake) — there's no physical foul or timeout button. `CLOCK_TOGGLE` and `SETTINGS` aren't game-state actions (the bridge isn't allowed to know if the clock is running), so they pass through as their own `CLOCK_TOGGLE`/`TOUCH_LOCK_TOGGLE` markers for later daemon glue to resolve. Has a first-class `devMode` that reads fake input from stdin instead of real serial — verified by piping 8 fake lines through it (handshake swallowed silently, 6 real messages mapped correctly, 1 garbage line logged a warning and didn't throw).
- [2026-09-12] Built `daemon/clock.js` — a drift-safe ticker that reports the REAL measured elapsed time between ticks (via `Date.now()`), not the nominal interval, fixing the old codebase's clock-drift-under-load bug. Verified with `scripts/test-clock.mjs`: 5 ticks at ~100ms intervals matched wall-clock time, and restarting after an 800ms idle gap reported ~99ms on the next tick instead of dumping the whole gap into it.
- [2026-09-12] Built `daemon/journal.js` — local (no network) crash-recovery log: every action is appended to `journal.ndjson` with a synchronous write + `fsyncSync` so it survives a process kill or power blip the instant `recordAction()` returns; every 50 actions (configurable) the full state gets written to `snapshot.json` and the journal is truncated, so replay after a crash is always bounded to "since the last snapshot," not the whole game. `reconstructState()` is the only place this module talks to `shared/state-engine.js` (`reduce` + `createEmptyState`) — it folds the journal back over the snapshot to rebuild exact state. Verified with `scripts/test-journal.mjs`: same-process reconstruction, a simulated crash (a fresh `createJournal()` on the same directory reconstructing purely from disk), a forced snapshot rotation, reconstruction combining the new snapshot with post-rotation actions, and a second simulated crash after rotation still reconstructing correctly — plus `clear()` removing the snapshot and resetting the journal to empty. Runtime `data/`/`data-test/` dirs are gitignored.
- [2026-09-12] Verified the real Supabase schema live (restored the paused `eoowagimooxsqcrrihbw` project to check) before writing any cloud code. Found real drift worth remembering: `games.status` only allows `'live'|'finished'|'completed'` (not `'archived'`); `game_actions.action_type` has no DB constraint but IS a closed de facto vocabulary the website/app actually filter on (`foul`/`timeout`/`rebound`/`steal`/`turnover`/`block`/`assist`/`substitution`/`jumpball`/`foul_drawn`) — of state-engine's own actions, only FOUL and TIMEOUT map onto it; `shot_events.client_event_id` exists but has no unique constraint, so it can't provide real write-idempotency yet; `games` UPDATE/DELETE are RLS-gated on `hostId = auth.uid()`, confirming this daemon can only ever work with a service-role client, never a browser-style session. Also caught a real gap in `wire-contract.js`: `GamesDataSettings` was missing `shotClockDuration` — added it.
- [2026-09-12] Built `daemon/cloud-sync.js` (Part A) — the one function (`onStateChange(newState, action)`) everything calls after `reduce()`: translates state into the frozen `CLOUD_EVENTS` payloads, broadcasts only what actually changed (`score_update` diffed field-by-field; `clock_tick` throttled to ~1/sec regardless of internal tick rate — a `FOUL` never triggers a spurious tick), persists `games.data`, and writes `game_actions` rows for foul/timeout and `shot_events` rows for attributed scores in non-quick mode (the real fix for the old daemon never writing `game_actions`). Failed writes queue for a 15s retry instead of crashing or vanishing. Verified live: 30 ticks over 3 real seconds produced exactly 3 `clock_tick` broadcasts, a foul broadcast only `score_update`, and a rejected no-op broadcast nothing. The actual authenticated write path is verified structurally (mocked client, correct table/row shapes) pending the real `SUPABASE_SERVICE_KEY` in `daemon/.env`, which Shrujal will add later — `daemon/.env` currently only has the public anon key (used for the read-only cloud-fallback test below).
- [2026-09-12] Built `daemon/resume.js` (Part B) — boot recovery: try the local journal first (`reconstructState()`, no network at all), fall back to the cloud `games` row if the journal has nothing but Supabase shows the game still live (coarse — last snapshot only, no roster), else start fresh. Verified against the real database: a live test row inserted directly via SQL (code `BXPI`, cleaned up after) was correctly reconstructed through the public-anon-key read path — score, fouls, timeouts, period, possession all matched, clock always resumes paused, and roster correctly comes back empty (a real, documented limit of this fallback path, not a bug). Local-journal path separately verified with a simulated crash/restart never touching `supabaseClient` at all.
- [2026-09-12] Closed out the Task 5 review: (1) confirmed via `src/services/statsEngine.ts`'s own comment ("all basketball scoring... flows through shot_events") that there's deliberately no 'score' word in the game_actions vocabulary — shot_events already IS the scoring record for both stats and advanced mode (this codebase already writes it that way, gated on `!== 'quick'` not `=== 'advanced'`), so quick mode having zero play-by-play is correct ecosystem behavior, not a gap. (2) Added an application-level idempotency guard for shot_events writes (`insertShotEventIdempotent` in `cloud-sync.js`): a stable `client_event_id` generated once per logical write (reused across retries) is checked against existing rows before inserting, since the column has no unique DB constraint to lean on. Verified with a new `scripts/test-cloud-sync-idempotency.mjs`, simulating the exact failure this guards against (row lands, response errors, retry fires) — confirmed exactly one row survives. Also made `createCloudSync`'s retry interval configurable (`retryIntervalMs`) so tests don't wait the real 15s. (3) Standing rule going forward: flag Shrujal before any live-infra-touching action, every time, not just once per task — saved to memory. (4) Live write-path test against the real database is next, pending `SUPABASE_SERVICE_KEY` in `daemon/.env`.
- [2026-09-12] Task 6 (UI) stopped before starting: Tasks 1-5 built five independently-tested modules but nothing ran them together — no Socket.io server existed anywhere, so there was no real daemon for a UI to connect to yet. Flagged rather than building a UI against nothing (or quietly inventing daemon glue inside a UI task). Also surfaced: touch-lock had no wire event at all (`pico_status` only carries Pico connectivity), and `CLOCK_TOGGLE`'s start/stop resolution was explicitly deferred to daemon glue that didn't exist.
- [2026-09-12] Built `daemon/index.js` (Task 6a) — the orchestrator. ONE `dispatch()` funnels every action through `reduce()` → `journal.recordAction()` → `cloudSync.onStateChange()` → broadcast `STATE_UPDATE` (+ `SCORE_PENDING` only when a dispatch freshly creates a pending attribution, not on every later action while one sits unresolved), regardless of whether the action came from the Pico or the UI. `CLOCK_TOGGLE` (the Pico's one physical clock button) is resolved here — and only here — by reading `clock.isRunning` immediately before dispatching `CLOCK_START`/`CLOCK_STOP`. Touch-lock is kept as daemon-local state entirely separate from state-engine's `State`, broadcast on its own new `TOUCH_LOCK_STATUS` wire event (added to Contract B). Two small pieces of plumbing added and flagged, not silently invented: a non-uniqueness-checked placeholder game-code generator (real generation needs a Supabase uniqueness check, later work), and a `current-game-code.txt` breadcrumb file so a cloud-fallback boot resume actually has a code to look up (nothing before this task persisted one anywhere durable outside the journal itself).
- [2026-09-12] Verified `daemon/index.js` live end-to-end via a new `scripts/daemon-test-harness.mjs` (spawns the real daemon as a child process, drives it exactly like real hardware/UI would — stdin for devMode Pico input, a real Socket.io client). Found and fixed two real Node.js gotchas along the way, not daemon bugs: (a) a spawned child's stdout/stderr must go to real files, not `stdio:'pipe'` — Node can buffer pipe writes internally and never flush them while the process stays alive; (b) once the clock is running, `CLOCK_TICK` broadcasts arrive independently, so "wait for the next `state_update`" isn't safe anymore — added a predicate-based `waitForState()` alongside the simpler `nextStateUpdate()`. Confirmed: one dispatch path (Pico-originated and UI-originated `SCORE` produce byte-identical resulting team state — tested with `SCORE`, not `FOUL` as literally suggested, since the real Pico has no foul button at all); `CLOCK_TOGGLE` correctly alternates start/stop; touch-lock genuinely blocks `ui_action` while locked, unlocks/locks via the real `SETTINGS` toggle, and never appears inside a `state_update` payload; all three boot-resume paths through the real daemon (local journal, cloud fallback against a live test row inserted/cleaned up via SQL, fresh start); and a full end-to-end smoke test (setup → score → foul → clock start/stop) with the local pipeline confirmed live — the actual Supabase-row landing is still the same open item as Task 5, pending `SUPABASE_SERVICE_KEY`.
- [2026-09-12] Scaffolded `ui/` — the piece Task 6 was blocked on (no React/Vite/TS project existed anywhere in this repo before now). Standard `create-vite react-ts` scaffold, renamed to `box-pi-ui`, default template cruft removed. Placeholder files for all 8 Task 6 screens under `src/screens/`. `src/lib/socket.ts` imports `LAN_EVENTS` directly from `../../../shared/wire-contract.js` (not copied) — needed `allowJs` in `tsconfig.app.json` and an explicit Vite `server.fs.allow` for the parent directory, since `shared/` sits outside `ui/`'s own project root. `App.tsx` connects to the real daemon on `:3001` and logs/renders `state_update`/`game_ready` as a debug panel — no fake timer, no mock server. Verified fully live (not just "npm run dev looks fine"): booted the real daemon, ran the real Vite dev server, opened the actual page in a real headless Chromium via Playwright (now a dev dependency), and confirmed the browser's own console logged a real `state_update` payload broadcast by the daemon in response to a `setup_game` sent from a separate probe client — screenshotted for visual proof, then cleaned up. Real screen routing (Boot → Dashboard → ...) is still just BootSplash rendered unconditionally; that's later work, this stage was only the scaffold + wiring.
- [2026-09-12] Built `ui/src/screens/LiveGame.tsx` (Task 6b) — the main game screen, driven entirely by `state_update`/`SCORE_PENDING`/`TOUCH_LOCK_STATUS`, no independent logic. `ui/src/lib/daemonTypes.ts` is a hand-written TS mirror of `state-engine.js`'s State shape (JSDoc typedefs aren't consumable as real TS types) — while writing it, found the wire payload actually still carries `_previousState` (the internal undo snapshot) on every broadcast, a real daemon-side inefficiency flagged but not fixed here (out of scope). Touch-lock is enforced by exactly one full-screen overlay near the end of the render tree, not per-button checks. Court-tap "team confirmation" deliberately does NOT let the operator re-pick a different team's roster — the scoring team is already fixed the instant the physical button fired, and `ATTRIBUTE_SHOT`/cloud-sync always credit the original team regardless of what's sent; the toggle instead confirms which basket/orientation the tap represents, avoiding a worse team/player mismatch than the bug being fixed. Full courtZones.ts-accurate zone/arc geometry isn't ported into box-pi yet — the court surface here is a deliberate placeholder good enough to prove the confirm-before-commit flow, which is what the task was actually about.
- [2026-09-12] Found and fixed a real bug in `daemon/index.js` while exercising Task 6b's own verification (deliberately timing out an attribution popup, then scoring again): `dispatch()`'s `justBecamePending` only fired `SCORE_PENDING` on a null→non-null transition of `pendingAttribution`, but the reducer never clears it on its own (a popup timeout is UI-local only) — so a second unresolved score silently never got its popup, even though the score itself displayed correctly and the reducer had recorded a fresh `pendingAttribution`. Confirmed live before fixing (teamB's score updated, no popup ever appeared) and after (fixed instantly gates on `action.type === ACTIONS.SCORE` instead). Re-ran Task 6a's own dispatch-parity/clock-toggle/touch-lock tests after the fix — no regressions. Verified all 5 of Task 6b's scenarios live via a new permanent `ui/scripts/verify-livegame.mjs` (real daemon, real Vite server, real headless Chromium/Playwright): instant quick-mode scoring with no popup; stats-mode popup with correct countdown, a deliberate 8s timeout sending nothing, and a timely pick; advanced-mode court-tap proving nothing reaches the daemon until the player is confirmed (checked via the daemon's own log, not just the UI); a timeout button visibly disabled at zero remaining; and touch-lock genuinely blocking a click (Playwright's own actionability check refused it).
- [2026-09-14] Built `ui/src/screens/Spectator.tsx` (Task 6c) — the second-display, read-only screen. First factored LiveGame's inline score/clock JSX out into shared `components/ScoreDisplay.tsx` and `components/ClockDisplay.tsx` (plus `lib/format.ts` for `msToClock`) so both screens render from one place, not copy-pasted JSX — LiveGame now uses these too. Spectator has its own `state_update` subscription via the same shared `socket` module (not a poll, not derived from LiveGame) and renders nothing else — no popups, no controls, no touch-lock banner, no `lastError`. Added minimal URL-based routing in `App.tsx` (`/spectator` path) since the second display needs to actually be reachable as its own browser window — not the real 8-screen router, just enough to open both screens side by side for testing. Verified live with a new `ui/scripts/verify-spectator.mjs`: two separate Playwright pages (LiveGame + Spectator, each its own real Socket.io connection to the same daemon, exactly like two physical monitors would be) compared via a shared `data-testid="scoreboard"` region through a sequence of score/foul/timeout/clock-start actions — identical text at every step. Confirmed zero buttons/clickable elements on Spectator, and that tapping it causes no `state_update` (had to drain the fresh connection's own unsolicited initial `state_update` first, same gotcha as Task 6a's harness, or the probe's own connection looked like a false positive).
- [2026-09-14] Built `MatchSetup.tsx` + `RosterSetup.tsx` (Task 6d). Fixed a real wire-contract.js gap first: `SetupGamePayload` never had the `roster` field state-engine.js's SETUP_GAME action already accepted since Task 2 — added additively (Contract B). Caught the task's own spec text wrong while doing it: it said jersey `number` should be typed `number`, but state-engine.js's actual Player/normalizeRoster types (unchanged, already shipped) declare it `string` — followed the real reducer, not the task text, and flagged it in the typedef comment. MatchSetup sends `setup_game` immediately for quick mode (no `roster` key at all, not an empty one) or hands its fields to a small `Setup.tsx` wrapper for stats/advanced mode, which then shows RosterSetup; RosterSetup owns the actual combined send. Duplicate jersey numbers are blocked client-side per team (they'd make the Pico's stats-mode player picker ambiguous mid-game). Added minimal `/setup` URL routing in `App.tsx`, ordered so an already-active game always wins over sitting on the setup URL. Also found and fixed a real pre-existing bug while building these: the original create-vite scaffold's `index.css` still had light-theme `h1` color rules fighting every dark screen's own styling — invisible until these were the first screens to actually use `<h1>`; stripped `index.css` down to just the resets every screen needs. Verified live: quick mode sends immediately with an empty roster; stats mode correctly detours through Roster Setup and the resulting `state_update`'s `teamA/teamB.players` arrays were checked directly against what was typed in, not assumed; a duplicate jersey number was blocked with the button still clickable (needed so the error can actually be revealed — a `disabled` button can't be clicked to find out why); a forced `setup_error` (sending `setup_game` with a `null` payload through the page's own live connection, which is genuinely rejected by `reduce()`'s destructuring default only covering `undefined`) displayed inline on the real mounted MatchSetup component.
- [2026-09-14] Built `ui/src/screens/Settings.tsx` + `ui/src/screens/PostGame.tsx` (Task 6e), the last of Task 6's originally-listed 8 screens. Flagged and fixed a real gap in `daemon/index.js` first, with explicit approval before touching it: `wire-contract.js` had defined `GAME_ENDED`/`GameEndedPayload` since Task 1, but nothing in the daemon ever emitted it — `END_GAME` just flipped `meta.gameActive` false through the normal `STATE_UPDATE` stream with no dedicated signal. Added the emission inside `dispatch()`'s existing `ACTIONS.END_GAME` branch — cleans up `cloudSync`, clears the journal, clears the game-code breadcrumb, then emits `game_ended` with the just-ended code, after the final `STATE_UPDATE` (with the frozen final score) already went out, not before. Factored the shared modal chrome LiveGame's popups were already using out of `LiveGame.tsx` into `components/Overlay.tsx`, since Settings needed the same chrome — replaces the old `SettingsPanel.tsx` placeholder (deleted). Settings is reached from a new "⚙ SETTINGS" button in LiveGame's control row and rendered as an overlay, not a route — it gets touch-lock gating "for free" from the existing single full-screen lock overlay, no separate check added. End Game requires an explicit two-step confirm (a local `confirmingEnd` flag), not a single tap — considered and deliberately left out a manual period-advance override, since nothing in this project has actually asked for one. `App.tsx` now captures the final state via a `latestStateRef` updated on every `state_update` (needed because the `game_ended` handler is registered once with an empty dependency array, so `state` itself would be a stale closure there) and routes to `PostGame` whenever it's set; a fresh `game_ready` clears it so a stale post-game snapshot can't bleed into a new game. `PostGame` reuses `ScoreDisplay` with `hasBall={false}` for both teams (possession is meaningless once a game has ended) and Return-to-Dashboard does a full `window.location.href = '/setup'` navigation, not a state reset — guarantees no screen's local state survives into the next game; Dashboard itself still doesn't exist, `/setup` stands in for it same as Task 6d left it. Verified live via a new `ui/scripts/verify-settings-postgame.mjs` (real daemon, real Vite, real headless Chromium): opening and closing Settings mid-game left the scoreboard byte-identical; a single tap on "End Game" only reached the confirm step and left the game running (Live Game still mounted, confirmed via `data-testid="scoreboard"` presence) until "Yes, end game" was actually clicked; Post-Game's displayed score matched the real last `state_update` received before `game_ended` fired, not an assumption; and Return-to-Dashboard led to genuinely empty MatchSetup fields, then a freshly-started game confirmed at 0-0 with the new team names and period 1 — no leftover state from the previous game. Re-ran all three of Task 6a's daemon regression tests (dispatch-parity, clock-toggle, touch-lock) after the `daemon/index.js` change — no regressions. One iteration needed along the way: an early version of the verification script's "Settings overlay gone" check false-failed because it substring-matched the page body for "SETTINGS", which the always-present "⚙ SETTINGS" button text itself satisfies whether or not the overlay is open — fixed by checking for the `SETTINGS` heading specifically, a script bug, not a product bug.
- [2026-09-14] Built `ui/src/screens/Dashboard.tsx` (Task 6f) — the real landing screen. Found and resolved a genuine conflict between the task text and the existing `App.tsx` routing before writing any Dashboard code: `App.tsx` was already auto-routing straight to `LiveGame` the instant `state.meta.gameActive` was true, which would make the task's own "resumed-boot shows a distinct banner" requirement unreachable — the daemon resuming an already-active game on boot (Task 5/6a's logic) would jump past Dashboard before it ever got a chance to check anything. Fixed by adding a `confirmedLive` gate in `App.tsx`: the auto-jump to `LiveGame` now only fires once THIS session has explicitly caused it — either a `GAME_READY` (a fresh game this session started) or the operator tapping through Dashboard's resume banner — so a silently-resumed boot now correctly lands on Dashboard first. Dashboard itself takes `connected`/`state` as props from `App.tsx`'s already-central socket subscription (same pattern as `PostGame`, not a second independent subscription) and branches on `state.meta.gameActive`: true shows a `data-testid="resume-banner"` card with team names/period and a "Resume Live Game" button; false shows the normal "Start New Game" button, which does a full `/setup` navigation. Connection status reuses the exact scaffold-stage `socket.connected` + connect/disconnect-listener pattern from the original Task-scaffold `App.tsx` (recovered from `git show` of that commit — it had been dropped when `App.tsx` was rewritten for real routing in a later task, without anyone re-adding a visible connection indicator anywhere), lifted back into `App.tsx` state and passed down as a prop rather than reinvented. Also found and fixed a real, previously-masked bug while building this: `LiveGame` initialized its own `state` to `null` and only ever populated it from a FUTURE `state_update` broadcast — since it reuses the already-connected app-wide `socket` singleton rather than opening its own connection, it had no way to pick up state the daemon already reported to `App.tsx` before `LiveGame` mounted. Every earlier task's verification happened to follow a game-start with an extra action (a score, a foul) that produced a fresh broadcast and masked this; Dashboard's resume flow (Resume Live Game -> LiveGame, no intervening action) is what actually exposed it. Fixed by giving `LiveGame` an optional `initialState` prop, seeded from `App.tsx`'s already-current `state`. Verified live via a new `ui/scripts/verify-dashboard.mjs`, booting two separate real daemons (real Vite, real headless Chromium) since "did the daemon resume a game" is decided once, at boot: Part 1 — a fresh boot with no prior journal shows the normal start flow, reaches Match Setup on click, and the connection-status line flips to "disconnected" within the same page when the real daemon process is killed underneath it; Part 2 — a journal + `current-game-code.txt` breadcrumb written to disk before the daemon boots (same technique `scripts/test-daemon-boot-resume.mjs` already uses) produces a real resumed-active boot, confirmed via the daemon's own boot-resume-source log line, not assumed — Dashboard correctly shows the resume banner with the right team names/period instead of a blank Match Setup, and clicking through lands on LiveGame with the correct resumed score already rendered.
- [2026-09-14] Built the real `ui/src/screens/BootSplash.tsx` (Task 6g), unblocked once `/design-login` ran — imported Claude Design project "Splash screen refinement for Waveshare" (`Splash Screen v8 Turbine.dc.html`) via the `DesignSync` MCP tool and ported it faithfully: the animation math (ring rotations, radial oscilloscope wave, stage-segment fill sequence, ripple timing, comet/pin sweep) is copied unchanged from the source's `componentDidMount`/`start`/`tick`/`paint`/`toEnd` methods, converting only the WRAPPER — refs + `requestAnimationFrame` with direct `setAttribute()`/`style` mutation, same pattern the source already used via `querySelector`, never pushed through React state/re-render. All static SVG geometry (brackets, rivets, ring ticks, the 6 stage segments, gear teeth, orbit markers) is computed once via a pure `computeGeometry()` — a straight port of the source's `renderVals()` — and rendered as plain `.map()`'d JSX rather than the source's `<sc-for>` template loops. Kept the fixed 1024×600 canvas exactly as designed (built entirely from fixed-pixel coordinates for a specific target panel) rather than rescaling it, to stay pixel-identical; centered it in a full-viewport wrapper instead. Added the two Google Fonts (JetBrains Mono, Archivo) to `index.html`'s `<head>` — the repo's first screen to need custom fonts, so this establishes rather than follows an existing convention. `opId` (top-right OP_ID readout, QR caption) kept as the source's own placeholder value (`K7QM`) — flagged rather than invented, since there's no real box-level device id yet, only per-game codes. No App.tsx changes were needed to wire the transition-off behavior: App.tsx already renders BootSplash only as the final fallback and switches away the instant ANY `state_update` has arrived — a strictly broader condition than "GAME_READY or a resumed-active state_update" (the literal task wording), since a genuinely fresh boot with no active game must also leave this screen, and already does, straight to Dashboard. The "keep looping/holding rather than erroring if the daemon takes unusually long" requirement is likewise already satisfied for free — the animation's `autoReplay` loop was never tied to any real timer, so it just keeps cycling for as long as this screen stays mounted. Verified two ways: visually, screenshotting the animation at several points in its cycle (spin-up, mid-load, the wordmark/QR/ENTER end screen) against the live source file rendered separately, confirmed matching; and behaviorally via a new `ui/scripts/verify-bootsplash.mjs` (real daemon, real Vite, real headless Chromium) — BootSplash renders with no daemon connection, is still showing (not stuck/errored) after outlasting one full ~5.5s animation cycle with the daemon still down, and unmounts automatically the moment a real daemon is started and its first `state_update` arrives, landing cleanly on Dashboard. Also ran a full production build (`npm run build`) to confirm the heavy per-frame SVG attribute mutation compiles and bundles cleanly, not just under the dev server.
- [2026-09-14] Real boot progress on the Splash screen. Added `LAN_EVENTS.BOOT_PROGRESS`/`BootProgressPayload` to `wire-contract.js` Contract B (`stage: 'resuming_state'|'starting_server'|'connecting_uart'|'starting_clock'|'ready'`, `percent`, optional `detail`) — deliberately separate from `GAME_READY`, which means "a setup_game succeeded," a different real-world moment from "the daemon itself finished booting." `daemon/index.js`: moved `httpServer`/`io` creation earlier (before boot-resume runs, not after) so an `emitBootProgress()` helper exists from the very first real stage; emits `resuming_state`(0%) before `resumeGameState()`, `starting_server`(25%, with the resume source as `detail`) once it resolves, `connecting_uart`(50%) once `httpServer.listen()` succeeds, `starting_clock`(75%) once the uart-bridge is created, `ready`(100%) once `ticker.start()` runs — moved uart-bridge creation and `ticker.start()` out of module scope into the `listen()` callback so their completion is what these stages are actually tied to, not a timer. Every new `io.on('connection', ...)` immediately gets the CURRENT stage/percent (same pattern as the existing `state_update`/`touch_lock_status` snapshot-on-connect), so a late-joining client never sees a stuck 0% — it sees `ready`/100 straight away if the daemon already finished booting. Added a test-only `BOX_PI_TEST_BOOT_DELAY_MS` env hook (mirrors `BOX_PI_DATA_DIR`'s existing pattern) to artificially delay the boot sequence just before the uart-bridge stage, for exercising the "boot takes a while" path without real slow hardware. `BootSplash.tsx`: reused the existing single `e` (0-1) scalar that already drove the arc/comet/segments/hub (per the task's own instruction not to add a second, competing value) — it's now chased via `requestAnimationFrame`-driven exponential smoothing toward the real `percent` instead of a fixed-duration timer, so a 25%-wide stage jump animates smoothly rather than snapping. `App.tsx` owns the `BOOT_PROGRESS` subscription centrally (same pattern as its other listeners) and gates the Dashboard branch behind a new `bootReady` flag, set only once `BootSplash`'s own `onReady` fires — `state` truthiness alone was too early a signal (the daemon already pushes an initial `state_update` on connect before its own uart-bridge/clock have actually started). Found and fixed a real bug in the interpolation itself: pure exponential decay asymptotically approaches but mathematically never exactly reaches the target, so the readout could hover at 99% forever — fixed with a snap-to-target once within 0.5 of it, and a separate snap-immediately-on-first-payload rule (the very first `BOOT_PROGRESS` a client receives is a connect-time snapshot of wherever the daemon already is, not a transition from a previously-observed value, so smoothing FROM 0 there was wrong and made a late joiner's "already ready" state visually climb for ~3s instead of showing 100% immediately). The 3-second minimum display time (`elapsedMs >= 3000 && stage === 'ready'`) is enforced **unconditionally**, exactly as the task specified ("holds for the full 3 seconds even if boot finishes in under a second") — including for late joiners and, as a direct consequence, every full-page navigation within an already-running daemon (Return-to-Dashboard, Start New Game), since box-pi navigates between major screens via `window.location.href` reloads rather than client-side routing; flagged in code comments as a known, deliberate tradeoff rather than silently special-cased away, since an earlier attempt at exactly that special-casing was reverted after re-reading the task's explicit wording. Verified live via a new `ui/scripts/verify-boot-progress.mjs` (real daemon, real Vite, real headless Chromium, plus a raw `socket.io-client` probe to inspect the actual event sequence directly): a fresh boot shows real distinct stage transitions and holds the full 3s minimum even though actual boot finishes well under that; `BOX_PI_TEST_BOOT_DELAY_MS`-simulated slow boot shows real progress for longer than 3s with no artificial cutoff; a late-joining client's bar shows `ready`/100 immediately (not stuck at 0%) while still honoring the unconditional 3s floor before navigating; a resumed-active-game boot still lands on Dashboard first with the resume banner, not directly on Live Game. Re-ran all three Task 6a daemon regression tests after the `daemon/index.js` restructuring — no regressions. While regression-testing the other UI scripts this touched, found and fixed a real, pre-existing bug in `verify-settings-postgame.mjs` unrelated to this task: it unlocked touch based on an admin probe socket's own independent `state_update`, which can resolve before the browser has additionally received and processed the separately-sent `game_ready` — racing ahead could unlock while the browser was still momentarily rendering Setup (App.tsx's `LiveGame` condition needs both `gameActive` and `confirmedLive`/`game_ready`), and LiveGame's real mount moments later started fresh with `touchUnlocked` back at its default `false`, silently relocking; fixed by waiting for the browser's own `scoreboard` element instead of the admin socket's copy. Also found that same script's Scenario 4 had gone stale since Task 6f changed Return-to-Dashboard's target from `/setup` to `/` (Dashboard) — the script still expected direct `/setup` navigation; updated it to match the real, intentional flow (Return to Dashboard -> Dashboard -> Start New Game -> Match Setup).
- [2026-09-14] Fixed real problems with the boot-progress animation the user actually observed running it: sometimes stuck on the wordmark/CTA end screen forever, sometimes snapping to 100% almost instantly instead of animating. Bug 1 (the stuck case, critical): `tick()`'s loop-stopping condition (`e >= 0.999 -> toEnd(); return`) was gated purely on the VISUAL percent, with no check that `onReady()` had actually fired yet — if the visual dial cosmetically reached ~100% before real-ready + `MIN_MS` were both satisfied, the rAF loop died permanently with nothing left to ever call `onReady()` again, stranding the app on this screen for good. Fixed by gating that stop condition on `readyFiredRef.current` — the loop now only stops once `onReady()` has already fired. Bug 2 (the instant-snap case): real daemon stages can all complete within a second or two on a fast machine, giving the old pure-exponential-smoothing nothing to visually chase. Added a deliberate visual pace (`loadPaceMs`, derived from `MIN_MS - effectivePrimeMs` so the wheel's paced sweep finishes at the exact same moment the ready-gate can open, not up to a second earlier — a real misalignment bug in the fix as first drafted) that caps the effective target FROM ABOVE by both the real reported percent (a genuinely slow boot is never rushed past stages that haven't completed) and the pace, and FROM BELOW by whatever's already displayed (so it can only ever move forward, never backward — this floor turned out to be essential, not optional: without it, the late-joiner snap-to-real-value logic and the new pacing fought each other on alternating frames, undoing the snap). `MIN_MS` bumped from 3000 to 4000 to match. Also implemented the user's own explicit follow-up on the earlier "unconditional minimum" tradeoff flagged in the previous entry: a `sessionStorage` flag (`box-pi-splash-shown`, set once a real boot sequence genuinely completes) now lets a REPEAT view within the same browser session skip both the pacing and the `MIN_MS` floor entirely and land on Dashboard the instant the daemon reports ready — session-scoped deliberately, not `localStorage`, so an actual device/browser restart (the real cold boot a kiosk experiences) gets the full animation again. This exposed a genuine design conflict that needed resolving, not just applying the suggested patch verbatim: the existing late-joiner "snap directly to the real value on the first payload received" logic (added in the previous entry to satisfy "a late joiner sees ready/100 immediately, not stuck at 0%") was firing on every fresh-session load too whenever the daemon happened to already be fully booted before the page connected — which, in testing, turned out to be the common case, and is exactly what reproduced the "sometimes instant" symptom the user reported. Resolved by scoping that snap to ONLY the repeat-session case (`hasShownSplashThisSession()`) — a genuine first-session view now always shows the deliberate paced sweep, even if the daemon happens to already be fully ready, per the task's own explicit wording ("even if boot finishes in under a second"). Verified live via a new `ui/scripts/verify-boot-pacing.mjs`: 6 repeated fresh-boot runs (fresh browser context each time, same long-running already-booted daemon reused — deliberately the worst case for snapping) all showed genuine paced climbing (consistently ~4-5% at 1.6s, ~50-52% at 3.2s across all 6 runs, not 0-then-100) and reached Dashboard in a bounded ~4.3-4.5s, never stuck — repeated specifically because the original bug was intermittent, not reproducible on a single run; a slow boot (`BOX_PI_TEST_BOOT_DELAY_MS=6000`, longer than `MIN_MS`) showed the wheel correctly capped at 49-50% at the 4.5s mark rather than rushing to 100%, and took the full real ~5.9s, not an artificial 4s cutoff; the same browser context loaded twice showed a full ~4.3s first load followed by a ~130ms second load (session-skip working); a brand-new context after that got the full animation again (~4.4s), confirming the flag is session-scoped, not permanent. Re-verified `verify-boot-progress.mjs`, `verify-bootsplash.mjs`, `verify-dashboard.mjs`, and `verify-settings-postgame.mjs` all still pass — `verify-boot-progress.mjs`'s own late-joining-client scenario needed updating to match the corrected semantics (a late joiner in a genuinely fresh session now correctly gets the paced sweep too, not an instant snap — only a same-session repeat view skips it), and its 3000ms-based thresholds were bumped to match the new `MIN_MS`. No `daemon/index.js` changes this round — daemon regression suite not re-run.
- [2026-09-14] Full audit of `BootSplash.tsx` against an authoritative ground-truth spec for the source design (user provided the exact math directly, not reverse-engineered), fixing the real root cause behind the previous fix's own bug: "the blue wheel spins 5 times separately" instead of turning through one continuous sweep. Audited every section of the current file against the spec — geometry (brackets/rivets/majors/teeth/orbit dots), the 5 rotation ratios (sweep ×1.0, outer ring ×0.25, hub teeth ×−0.6, orbit dots ×0.5, inner dashes ×−0.75), the progress-arc dasharray math, stage segment/kick/ripple logic, the oscilloscope, and END-phase timing were all confirmed to already match the spec exactly — no changes needed there. The actual divergence was isolated entirely to how `e` (the single scalar driving `spin` and everything else) got computed during the LOAD phase: the previous fix derived it by exponentially smoothing `displayedPercentRef` toward whatever the real `BOOT_PROGRESS` percent currently was — since that real value only updates in discrete steps whenever a real daemon stage lands, `e` (and therefore every rotating layer, all of which are fixed ratios of the same `spin`) advanced in visible bursts timed to backend events rather than one smooth curve — the actual root cause of "spins several times separately," not a coincidence that it roughly matched the daemon's 5 real stages. Fixed by replacing the exponential chase with the source's own pure time-based formula: `t = loadElapsedMs / LOAD_DURATION_MS` (now a real constant, 4400ms, not the earlier 3000/4000 guesses) clamped to 1, `e = 1-(1-t)^3` (ease-out cubic) — completely decoupled from when real backend events arrive. The real reported percent is now used ONLY as a ceiling (`e = Math.min(pacedE, realCeiling)`): it can hold the sweep back on a genuinely slow boot (never rushed past stages that haven't completed) but can never make a fast boot look faster than the deliberate pace. Proved monotonic by construction — since both `pacedE` and `realCeiling` are individually non-decreasing over one boot sequence, their `min` is too, so the earlier fix's explicit floor-clamp (needed only because the exponential-chase approach could otherwise regress) is no longer necessary and was removed. `MIN_MS` reconciled to `PRIME_MS + LOAD_DURATION_MS` = 5500ms, computed from those two constants rather than hand-picked a third time, so it can't drift out of sync with what the animation actually takes to visually finish. The `hasShownSplashThisSession()`-gated late-joiner snap (repeat session view only) is unchanged from the previous fix — still correct, since under the new ceiling-based formula a first-session view is naturally unaffected by the real percent being already high (the ceiling just doesn't bind), so no extra special-casing was needed there. Verified: re-ran `ui/scripts/verify-boot-pacing.mjs` (6 repeated fresh-boot runs, slow-boot capping, session-skip, new-session-reset) — all pass, now showing a proper decelerating ease-out curve (e.g. 26-29% at 1.6s-post-PRIME rising to 84-85% at 3.2s, not the previous fix's flatter/steppier profile) and correctly longer ~5.9-6.1s totals matching the new 5500ms `MIN_MS`. Built a new dedicated `ui/scripts/verify-boot-continuity.mjs` specifically targeting the reported symptom: samples the percent readout every 200ms through the whole LOAD phase and asserts small, steady per-sample deltas rather than big jumps — actual samples came back as a textbook ease-out-cubic curve (11, 24, 34, 44, 54, 61, 68, 74, 79, 84, 87, 90, 93, 95, 96, 98, 98, 99, 99, 99), monotonically increasing, max single-step delta 13%, at most one brief flat stretch right at the very end — confirming the fix directly, not just inferring it from pass/fail on higher-level checks. Also explicitly ruled out the "SVG `<text>` renders as an invisible span" gotcha flagged in the spec (specific to the OLD x-dc canvas runtime) by reading every SVG text node's actual rendered content/visibility from the live DOM — all 10 (housing numerals 000/090/180/270, stage numbers 01-06) render real, visible text; confirmed not applicable to this React port, as expected, since React handles SVG text interpolation natively with no separate hydration step. Re-verified `verify-boot-progress.mjs`, `verify-bootsplash.mjs`, `verify-dashboard.mjs`, and `verify-settings-postgame.mjs` (all still pass — their existing thresholds already had enough tolerance to absorb `MIN_MS` growing from 4000 to 5500) and a full production build. No `daemon/index.js` changes — daemon regression suite not re-run.
